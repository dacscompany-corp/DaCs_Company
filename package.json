{
  "scripts": {
    "test": "node tests/money-math.test.js",
    "build": "node -e \"console.error('\\n*** BUILD DISABLED (2026-07-03) ***\\njs/portal-app.compiled.js IS the source of truth — edit it directly.\\nsrc/portal-app.jsx went stale on 2026-06-03 (missing labor contracts + later fixes);\\nrebuilding from it would wipe the live portal. See src/README.md.\\n'); process.exit(1)\"",
    "build:watch": "npm run build",
    "build:prod": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "jimp": "^1.6.0"
  }
}
